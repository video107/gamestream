<div class="container">
  <div class="row">
    <h2>Hi <%= current_user.user_name %></h2>
    <%= form_for @case, :url => menu_cases_path(@menu) do |f| %>
      <%= f.hidden_field :case_url%>
      <%= f.submit "建立專屬連結", :class => "btn btn-primary" %>
      <%= link_to "回到遊戲頁面",root_path,:class => "btn btn-default" %>
    <% end %>
  </div>
</div>