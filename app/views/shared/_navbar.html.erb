  <nav class="navbar navbar-inverse nav_bar">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Brand", root_path, class: "navbar-brand"%>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-nav-customizd">
          <li><%= link_to "關於我們", about_path %></li>
          <li><%= link_to "直播主招募", recuit_path %></li>
          <li><%= link_to "Q&A", qa_path %></li>
          <% if current_user %>
            <li class="promote_li"><%= link_to "推廣任務", menus_path %></li>
            <li class="promote_li"><%= link_to "推廣成效", user_cases_path(current_user) %></li>
          <% end %>
          <li><%= link_to "聯絡我們", contact_path %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right login_nav">
          <% if current_user %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  <% if current_user.fb_image %>
                    <%= image_tag(current_user.fb_image,size: "60x60", :class => "img-circle") %>
                  <% else %>
                    <%= current_user.user_name %>
                  <% end %>
                  <b class="downArrow"></b>
              </a>
              <ul class="dropdown-menu">
                  <li><%= link_to "個人資料", edit_user_path(current_user) %></li>
                  <li><%= link_to "推廣任務", menus_path %></li>
                  <li><%= link_to "推廣成效", user_cases_path(current_user) %></li>
                  <li role="separator" class="divider"></li>
                  <li><%= link_to('登出', destroy_user_session_path, :method => :delete) %></li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "直播主登入", new_user_session_path, class: "login_nav" %></li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
  </nav>
